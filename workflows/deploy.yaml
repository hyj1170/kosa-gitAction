name : Github Action  실행시켜 보기

on:
  push:
    branches:
      - main


jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest
    steps: #첫번째 스텝
      - name: Hello World 찍기
        run : echo "Hello World"

      - name: 여러 명령어 문장 작성
        run: |
          echo "Good"
          echo "Morning"
       
      - name: secret 값 설정
        run:
          echo ${{secrets.MY_NAME}}
          echo ${{secrets.MY_HOBBY
